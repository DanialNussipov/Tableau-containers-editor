version: '3.8'

services:
  web:
    build: .
    container_name: tableau_containers_app
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./src:/app/src
    restart: unless-stopped